#!/usr/bin/env node
const query = process.argv.slice(2)[0],
    Roll = require(`${__dirname}/../lib/index`).default,
    roll = new Roll();

try {
    console.log(roll.roll(query).result);
} catch (error) {
    console.error(`${error.name}: ${error.message}`);
    process.exit(1);
}
